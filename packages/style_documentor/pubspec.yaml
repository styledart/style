name: style_documentor
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.15.0-118.0.dev <3.0.0'


dependencies:
  http: ^0.13.4
  source_gen: ^1.1.0
  analyzer: ^2.3.0
  build: ^2.1.0
  # Not imported in code, but used to constrain `build.yaml` requirements
  build_config: ^1.0.0



dev_dependencies:
  build_runner: ^2.1.2
  lints: ^1.0.1
  test: ^1.17.12
